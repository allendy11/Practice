const db = require("../db");

// module.exports = {
//   get: {
//     user: (cb) => {
//       const queryString = `select * from users`;
//       db.query(queryString, (err, result) => {
//         cb(err, result);
//       });
//     },
//   },
// };
